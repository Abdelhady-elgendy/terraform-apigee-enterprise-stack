# Quickstart

Start with `examples/single-region`.

## Steps
1. Fill `terraform.tfvars`
2. `terraform init`
3. `terraform plan`
4. `terraform apply`

## Notes
Terraform Stacks can orchestrate components; this repo keeps components modular to work both inside and outside stacks.
